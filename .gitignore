# Ignorar diretório de dependências do Node.js
node_modules/

# Ignorar logs de debug do npm
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Ignorar arquivo de configuração do UniFi (contém credenciais criptografadas)
# Este arquivo deve ser gerenciado fora do Git, por exemplo, via volumes Docker
unifi_config.json

# Ignorar arquivos de sistema e editores comuns
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
.*.swp

# Ignorar o próprio Dockerfile e docker-compose.yml se você não quiser versioná-los
# (Geralmente você quer versionar estes, então estão comentados por padrão)
# Dockerfile
# docker-compose.yml